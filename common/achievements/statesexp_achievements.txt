unique_id = achievements_statesexp

no_more_division_test = {
    possible = {
        difficulty > 1
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
        original_tag = SYR
    }
 
    happened = {
        NOT = {
            has_idea = SYR_divide_and_rule
        }
        has_stability > 0.75
    } 
}